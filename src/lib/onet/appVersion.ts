/**
 * Версия игры (одна точка правды).
 *
 * Используется:
 *  - в маркере полной установки (localStorage + Cache API) — при смене версии
 *    приложение в фоне перекачивает файлы и применяет апдейт со следующего
 *    запуска (см. lib/onet/offline.ts);
 *  - в <meta name="kk-version"> (layout.tsx) — по ней клиент узнаёт, что на
 *    сервере появилась новая версия, НЕ перезагружая страницу.
 *
 * ПРАВИЛО: поднимаем на каждой сдаче вместе с package.json.
 */
export const APP_VERSION = '1.7.1';
